# AI File Organizer 用户使用手册

本文面向直接使用桌面界面的用户。程序的推荐工作方式是：**扫描 → 核对分类 → 必要时运行 AI → 预览 → 执行 → 按需撤销**。

> 安全原则：程序默认不永久删除文件、不覆盖同名文件、不修改受保护的系统目录，并且不会在未经预览的情况下执行整理。重命名、AI 分析、自动解压和解压后处理都是可选功能。

## 1. 首次启动

### 1.1 使用 Windows EXE

双击 `AI File Organizer.exe` 即可启动。单文件 EXE 首次打开时需要释放运行组件，可能比后续启动稍慢。程序使用单实例锁；如果已经打开，再次双击只会提示已有实例。

打包版把用户数据保存在：

```text
%LOCALAPPDATA%\AI File Organizer\
├─ data\       配置、SQLite 数据库和密码条目元数据
└─ logs\       滚动日志
```

移动 EXE 不会移动或清除这些设置。API Key 和解压密码不会写入 EXE、SQLite 或普通日志。

### 1.2 从源码启动

需要 Python 3.11 或更高版本：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
python main.py
```

也可以双击 `start.bat`。源码运行时，数据和日志默认位于项目的 `data/`、`logs/` 目录。

### 1.3 第一次使用建议

1. 先用一个包含少量副本文件的测试目录熟悉流程。
2. 打开“设置”，确认递归扫描、隐藏文件、历史记录和撤销选项。
3. 在“分类规则管理器”检查内置扩展名规则。
4. 暂时不要勾选重命名；先验证纯分类移动结果。
5. 需要 AI 时再从界面配置 API；没有 API Key 不影响本地规则功能。

## 2. 主窗口

- **左侧**：当前目录的文件夹树、规则入口和分类列表。
- **中间**：文件表格。可勾选、多选、排序、搜索、筛选，并手动编辑建议分类。
- **右侧**：当前文件的有限预览、原始元数据、分类来源、置信度、理由、建议名称和压缩信息。
- **底部**：扫描、停止、AI 分析、预览整理、执行、撤销、可选重命名、进度和运行日志。

图片预览有像素数和文件大小限制；文本只读取有限内容；PDF 不会在界面线程中完整解析。因此“无法预览”不代表文件损坏。

## 3. 扫描文件

1. 点击“选择文件夹…”并选择普通用户目录。
2. 点击“扫描”。大目录会在后台处理，界面不会因为扫描而冻结。
3. 需要中止时点击“停止扫描”；已经发现的结果会保留，但不会继续自动解压。

扫描记录包括文件名、完整路径、扩展名、大小、创建/修改时间、前缀和 MIME 类型。

### 扫描设置

- **递归扫描**：开启后扫描所有子目录；关闭时只扫描当前层。
- **扫描隐藏/系统文件**：默认关闭。开启前应确认目录内容，程序内部暂存目录始终不会进入扫描。
- **保留原目录结构**：分类目标下继续保留来源的相对子目录；程序会避免重复叠加相同分类目录。

磁盘根目录、系统目录、程序数据目录、应用自身文件、链接和重解析点会被安全拦截。文件在扫描后被替换或父目录被重定向时，解压、整理和撤销会重新校验并拒绝继续。

## 4. 分类规则与优先级

分类顺序固定为：

```text
用户手动分类 > 前缀规则 > 扩展名规则 > AI 判断 > 待确认
```

### 4.1 前缀规则

在“分类规则管理器”中可以新增、修改、删除、启用/禁用和调整优先级。例如：

```text
IMG_        → 图片
invoice_    → 财务
ProjectA_   → 项目A
```

前缀匹配默认忽略大小写；可在规则中单独切换。使用“测试规则”输入文件名即可查看命中结果，不会修改文件。

### 4.2 扩展名规则

图片、视频、音频、文档、表格、压缩包、安装包和代码等基础规则已内置，但都可以修改或删除。扩展名应包含点，例如 `.pdf`；复合扩展名如 `.tar.gz` 会作为整体识别。

### 4.3 手动分类和规则学习

双击表格中的“建议分类”可手动选择或输入类别。程序会记录相同前缀的手动选择趋势；达到提示条件时，会询问是否创建规则。只有点击确认后才会新增规则，拒绝后不会反复提示同一建议。

## 5. 在界面配置 AI API

API 不通过配置文件或源码填写。点击主窗口顶部的“API 配置…”或进入“设置 → API”。

先从“API 厂商预设”选择 OpenAI、DeepSeek、Kimi 中国区、阿里云百炼、硅基流动或智谱 GLM。程序会自动填写官方 OpenAI-compatible Base URL 和常用模型列表；如果使用代理、自建服务或其他厂商，选择“自定义（OpenAI 兼容）”。预设值仍允许手工修改，最终可用模型以账号权限和“测试连接”结果为准。

| 字段 | 说明 |
|---|---|
| Base URL | OpenAI-compatible 服务根地址，例如 `https://api.openai.com/v1` |
| API Key | 服务密钥，输入框默认掩码显示 |
| 模型 | 兼容服务提供的模型名称 |
| 超时 | 单次网络等待上限 |
| 并发数 | 同时分析的请求数；过高可能触发限流 |
| Temperature | 分类输出随机度，建议保持较低值 |
| Token 费率 | 可选，仅用于本地估算成本 |

填写后点击“测试连接”。成功会显示模型和延迟；失败会显示认证、地址、超时、限流或响应格式等具体原因。服务不提供 `/models` 时，程序会使用最小聊天请求验证兼容性。

安全限制：

- 非本机服务必须使用 HTTPS；HTTP 只允许 `localhost` 或回环地址。
- Base URL 不能包含账号、密码、查询参数或片段。
- API Key 按服务来源隔离，优先保存在系统 Credential Manager/Keychain；安全后端不可用时只保留在本次运行内。
- 没有 API Key 时仍可扫描、使用规则、解压、预览、整理和撤销。

## 6. AI 分析、隐私和成本

只有“待确认”且已勾选的文件会进入 AI 分析。点击“AI 分析”才会联网；停止分析或关闭窗口会取消在途请求和重试等待。

可发送的信息包括文件名、扩展名、大小、目录名称和有限内容。设置页可分别控制：

- 是否发送有限文本；
- 是否发送完整所在目录；
- 是否允许图片识别。

文本采用长度上限；PDF 只提取前几页；图片会缩放并重新编码为不含 EXIF/GPS 的 JPEG。文件内容是不可信输入，程序会要求模型忽略其中指令并严格返回 JSON；无效 JSON 只会令相应文件回到待确认，不会导致应用崩溃。

使用统计显示规则直接处理数、AI 文件数、Input/Output Tokens 和估算费用。服务没有返回 token usage 时显示“未知”，不会伪造数字。

## 7. 置信度

- 高于“自动建议阈值”：正常显示建议分类；默认阈值为 0.80。
- 介于低、高阈值之间：黄色提示，建议人工核对。
- 低于“待确认阈值”：保留为待确认；默认阈值为 0.50。

阈值可在“设置 → AI”修改。置信度只是辅助信息，不替代执行前预览。

## 8. 重命名是独立可选项

重命名不会随分类自动开启。要使用它，必须同时完成两步：

1. 在“重命名选项…”或设置中允许重命名并配置模板；
2. 在主窗口单独勾选“启用重命名（可选）”。

未勾选时只移动分类，AI 的建议名称不会生效。常用占位符：

| 占位符 | 含义 |
|---|---|
| `{date}` | 修改日期，格式 `YYYY-MM-DD` |
| `{year}` / `{month}` | 年 / 月 |
| `{category}` | 最终类别 |
| `{original_name}` | 原文件主名 |
| `{ai_title}` / `{title}` | AI 建议标题；没有时回退为原主名 |

示例：

```text
{date}_{category}_{original_name}
{category}_{ai_title}
{year}-{month}-{title}
```

Windows 非法字符、设备名、尾点和尾空格会被清理。目标已存在时自动生成 `report (1).pdf`、`report (2).pdf`，绝不静默覆盖。

## 9. 自动解压与密码

支持 `.zip`、`.7z`、`.rar`、`.tar`、`.tar.gz` 和 `.tgz`。ZIP/TAR 使用标准库，7z 使用 Python 库；RAR 需要可信的后端。Windows 上只检测安装在 Program Files 中的 7-Zip，不会从当前目录或 `PATH` 运行同名程序。

### 9.1 解压开关

- 自动识别压缩包；
- 自动解压（默认关闭）；
- 解压后继续扫描；
- 是否创建同名文件夹；
- 最大递归层数，`0` 表示不解压内层归档；
- 单包大小、文件数、压缩比和整个任务容量上限。

开启“自动解压”后，程序会固定递归查找**所选目录及其全部子目录**中的压缩包。这个压缩包发现流程独立于“文件 → 递归扫描”：即使普通文件只扫描当前层，深层目录中的压缩包仍会进入解压任务。解压得到的文件会按“解压后继续扫描”设置进入分类流程。

“最大递归层数”控制的是压缩包内部继续出现压缩包的层数，例如 `A.zip → B.rar → C.7z`，并不限制对所选文件夹子目录的查找深度。停止扫描后不会启动后续自动解压；停止解压会在安全边界处取消并保留明确的未完成状态。

### 9.2 解压位置

- **beside / 当前目录**：在压缩包旁创建同名文件夹；推荐默认值。
- **unified / 统一目录**：全部解压到指定普通用户目录。磁盘根、系统目录和应用数据目录会被拒绝。
- **temporary / 临时目录**：先在应用拥有的临时目录处理，关闭或开始新任务时清理。

目标重名时会生成带序号的新目录。无同名文件夹模式也采用暂存后无覆盖发布，不会直接覆盖已有文件。

### 9.3 密码管理

在“设置 → 解压 → 管理解压密码…”中维护密码、备注、启用状态和优先级。程序依次尝试：

```text
无密码 → 已启用密码 #1 → 密码 #2 → … → 标记需要密码/密码错误
```

不会进行字典攻击或暴力破解。右键压缩文件可立即解压、仅本次使用指定密码、保存新密码、重试、跳过或打开解压目录。只有明确勾选“保存到密码库”才保存。

密码优先进入系统安全凭据；`passwords.json` 只保存编号、备注、启用状态和顺序。界面日志只显示“密码 #2”，绝不显示实际值。

### 9.4 解压安全

程序会拦截 Zip Slip、绝对路径、`..`、盘符、NTFS ADS、设备名、尾点空格、链接、特殊文件和规范化重名，并检查声明值与实际写入量、实时磁盘空间及任务总预算。

扫描时会绑定压缩包的来源根和内容指纹；解压前、发布前以及原压缩包后处理前均重新检查完整父目录链。扫描后替换文件、把父目录改为链接或把路径重定向到扫描范围外都会显示“安全拦截”。

解压成功后的原包可选择：保留、移动到“已解压压缩包”或放入系统回收站。默认保留；程序不提供默认永久删除。

## 10. 预览、执行和撤销

### 10.1 整理预览

点击“预览整理”查看每一条：

```text
源完整路径 → 目标完整路径
```

在预览中取消不想处理的行。若目标分类仍为“待确认”，该文件不会进入计划。

### 10.2 执行整理

点击“执行整理”。启用“执行前必须确认”时会再次显示预览。每个文件独立执行：权限不足、被占用、已移动、目标竞争或身份变化只会令该行失败，不会中断其他文件。

跨卷移动会先写唯一临时文件、刷新到磁盘、校验内容，再无覆盖发布；中断后不会把半成品伪装为完成文件。每个文件的 pending/success/failed 状态会写入 SQLite，异常退出后下次启动会保守恢复日志状态。

### 10.3 撤销

“撤销上次操作”只处理最近一次成功操作，并先展示撤销预览。撤销前会验证：

- 原位置仍在记录的安全根内；
- 当前目标没有被链接重定向；
- 文件内容仍与执行时一致；
- 原位置没有出现同名文件。

不满足条件时拒绝自动移动并显示原因。放入系统回收站的原压缩包不承诺应用内自动撤销，可从操作系统回收站恢复。

## 11. 语言切换

应用支持简体中文、English、日本語和 한국어，对应内部代码为 `zh_CN`、`en`、`ja`、`ko`。可以从主窗口“语言”菜单切换，也可进入“设置 → 外观”选择；指南窗口上方还可以独立切换阅读语言。

切换后会立即重新翻译已打开的主界面和对话框，新选择也会保存。缺少的词条安全回退为中文；少量运行期组合消息、操作系统或解压后端的原始错误也可能保持原语言。语言设置只影响界面文字；用户自定义分类、文件名、路径、规则、历史数据和文件内容保持原文。

## 12. 材质包（外观包）

材质包只能提供一组界面颜色和一张可选背景图。程序会自行生成受控样式，包内不允许 QSS、Python、JavaScript、EXE、DLL、命令或网络内容。

### 12.1 导入与恢复

1. 打开主窗口“外观”菜单，或进入“设置 → 外观”。
2. `default` 是不附加 QSS 的原生 Windows 黑白系统控件风格；也可选择 `dark`、`ocean`、`warm`，或点击“导入”选择 `.aifopack` 文件。
3. 如果想自制材质，先用“生成示例包”得到一个可用模板，再修改清单和可选背景图。
4. 自定义包可删除，内置包不可删除。当已选包不存在或损坏时，界面应回退到 `default`。

导入采用“先完整校验、再发布”，失败不会破坏当前外观。已安装自定义包位于应用数据目录的 `material_packs/<id>/`。

### 12.2 包格式

材质包扩展名为 `.aifopack`，实际是一个严格白名单 ZIP。根目录只允许 `manifest.json` 和清单声明的一张 `.png` / `.jpg` / `.jpeg` / `.webp` 背景；没有背景时只包含清单。不要添加子目录、QSS、SVG、额外资源或隐藏载荷。

不含背景的最小结构：

```text
my-theme.aifopack
└─ manifest.json
```

`manifest.json` 必须是 UTF-8 JSON，且不允许未知字段。必填字段为 `format_version`（V1 必须为 `1`）、`id`、`name` 和 `colors`；可选字段为 `version`、`author`、`description`、`background`、`background_mode`。`id` 只能使用小写字母、数字、下划线和连字号；`background_mode` 只能是 `cover`、`tile` 或 `center`。

`colors` 必须正好包含以下颜色令牌，每个值只能是六位不透明色 `#RRGGBB`。V1 不接受八位透明色，避免不同 Qt 颜色通道顺序导致文字意外透明：

```text
window, panel, elevated, input, text, muted_text,
accent, accent_hover, accent_pressed, border, selection_text,
success, warning, danger
```

一个最小清单示例：

```json
{
  "format_version": 1,
  "id": "my-theme",
  "name": "我的材质",
  "colors": {
    "window": "#20242B",
    "panel": "#282D36",
    "elevated": "#303641",
    "input": "#191D23",
    "text": "#F2F4F8",
    "muted_text": "#AAB2C0",
    "accent": "#4F8CFF",
    "accent_hover": "#72A3FF",
    "accent_pressed": "#3974DB",
    "border": "#414957",
    "selection_text": "#FFFFFF",
    "success": "#42B883",
    "warning": "#F2B84B",
    "danger": "#EF6262"
  }
}
```

如需背景，在根目录放置一张受支持图片，并在清单中加入例如 `"background": "background.webp"` 和 `"background_mode": "cover"`。

### 12.3 导入安全限制

- 压缩包最大 12 MiB，`manifest.json` 最大 64 KiB，背景图最大 10 MiB，声明总解压量最大 11 MiB。
- ZIP 项目最多 3 个，单项压缩比不得超过 100；背景图单边不超过 8192 像素，总像素不超过 3200 万。
- 正文、辅助文字、选择文字、状态色和边框必须达到内置的最低可读对比度；颜色格式正确但会令界面不可读的包同样会被拒绝。
- 路径越界、链接/重解析点、重复名、加密项、未知压缩方式、压缩炸弹、额外文件和与清单不符的背景都会被拒绝。
- 导入报错时不要为了通过校验而改名或隐藏可执行内容；应从界面生成新示例包，只迁移颜色和背景。

## 13. 日志、数据和备份

- `organizer.db`：设置、规则、历史、手动分类学习和 API 用量。
- `passwords.json`：解压密码的非敏感元数据，不含密码正文。
- `app.log`：滚动日志；不会记录 API Key 或实际解压密码。

需要备份时，请先关闭应用，再复制整个 `data` 目录。系统凭据中的 API Key 和密码通常不包含在普通文件备份中，需要通过操作系统凭据工具单独管理。

## 14. 故障排查

### 无法启动

- 确认操作系统拦截提示并只运行可信构建。
- 源码版执行 `python -m pip check`，然后重新安装 `requirements.txt`。
- 若提示已有实例，先切回已打开窗口；确认没有窗口后再从任务管理器检查残留进程。

### 扫描结果为空或缺少文件

- 检查是否选择了系统/应用受保护目录。
- 检查递归扫描和隐藏文件开关。
- 链接、重解析点、内部暂存文件和扫描时无法读取的文件会被跳过。
- 文件可能在扫描过程中被其他程序移动；刷新目录后重新扫描。

### API 测试失败

- `401/403`：检查 Key、权限和所选服务来源。
- `404/405`：检查 Base URL 是否已经包含正确的 `/v1`；程序会自动尝试兼容回退，但不会猜测私有路径。
- `429`：降低并发数，等待服务限流窗口结束。
- 超时/断网：检查代理、防火墙和服务状态；本机服务可用 `http://127.0.0.1:端口/v1`。
- JSON 错误：确认模型支持 OpenAI-compatible chat completions，并尝试更稳定的模型或较低 Temperature。

### 压缩包无法解压

- “格式不支持”：RAR 后端或可信 7-Zip 可能未安装。
- “需要密码/密码错误”：确认密码启用状态和优先级，或右键仅本次输入。
- “安全拦截”：不要关闭限制直接重试；检查异常路径、链接、文件数量、容量、压缩比、磁盘空间或扫描后文件变化。
- 临时目录空间不足：释放系统盘空间，或改用同目录/统一目录模式。

### 文件整理失败

- 关闭正在编辑、播放或同步该文件的程序。
- 确认源目录和目标目录有写入权限。
- 如果目标在预览后被其他程序创建，重新预览以生成新的不重名名称。
- 出现“身份变化”时重新扫描，不要绕过校验。

### 撤销被拒绝

撤销是保守操作。文件被修改、目标不存在、原位置已占用、目录变成链接或历史缺少旧版安全信息时，程序不会猜测。请根据撤销预览和日志手动核对。

### 语言未完全刷新

先在主窗口“语言”菜单重新选择一次。用户自定义文本不会翻译；缺少词条、少量运行期消息和第三方后端或操作系统返回的错误可能保持中文或原始语言。

### 材质包导入失败或界面不可读

- 确认扩展名为 `.aifopack`，ZIP 根目录含 `manifest.json`，且只有清单声明的一张可选背景图。
- 检查必填字段、颜色令牌和六位 `#RRGGBB` 值；删除透明色、QSS、子目录、远程 URL、脚本、绝对路径和 `..`。
- 根据报错降低背景像素、图片大小或压缩包总体积；最稳妥的修复方式是在界面重新“生成示例包”后修改。
- 界面不可读时切回内置 `default`；材质包选择和导入不会修改规则、历史或任何用户文件。

## 15. 推荐安全习惯

1. 第一次整理重要目录前先备份。
2. 保持执行前确认、历史记录和撤销开启。
3. 不向不可信 API 发送文本、完整路径或图片。
4. 自动解压使用同名文件夹，并保持合理容量限制。
5. 不导入来源不明的材质包，不关闭安全拦截来处理可疑压缩包。
6. 每次都阅读整理预览，尤其是启用重命名或“解压后放入回收站”时。
